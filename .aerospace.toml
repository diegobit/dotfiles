# Place a copy of this config to ~/.aerospace.toml
# After that, you can edit ~/.aerospace.toml to your liking

# It's not necessary to copy all keys to your config.
# If the key is missing in your config, "default-config.toml" will serve as a fallback

# All possible keys:
# - Letters.        a, b, c, ..., z
# - Numbers.        0, 1, 2, ..., 9
# - Keypad numbers. keypad0, keypad1, keypad2, ..., keypad9
# - F-keys.         f1, f2, ..., f20
# - Special keys.   minus, equal, period, comma, slash, backslash, quote, semicolon, backtick,
#                   leftSquareBracket, rightSquareBracket, space, enter, esc, backspace, tab
# - Keypad special. keypadClear, keypadDecimalMark, keypadDivide, keypadEnter, keypadEqual,
#                   keypadMinus, keypadMultiply, keypadPlus
# - Arrows.         left, down, up, right

# All possible modifiers: cmd, alt, ctrl, shift

# All possible commands: https://nikitabobko.github.io/AeroSpace/commands

############################
# SYSTEM
############################
# Start AeroSpace at login
start-at-login = true

# Normalizations. See: https://nikitabobko.github.io/AeroSpace/guide#normalization
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# See: https://nikitabobko.github.io/AeroSpace/guide#layouts
# The 'accordion-padding' specifies the size of accordion padding
# You can set 0 to disable the padding feature
accordion-padding = 30

# Possible values: tiles|accordion
default-root-container-layout = 'tiles'

# Possible values: horizontal|vertical|auto
# 'auto' means: wide monitor (anything wider than high) gets horizontal orientation,
#               tall monitor (anything higher than wide) gets vertical orientation
default-root-container-orientation = 'auto'

# Possible values: (qwerty|dvorak)
key-mapping.preset = 'qwerty'

# Mouse follows focus when focused monitor changes
# See https://nikitabobko.github.io/AeroSpace/guide#on-focus-changed-callbacks
# See https://nikitabobko.github.io/AeroSpace/commands#move-mouse
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']

# Gaps between windows (inner-*) and between monitor edges (outer-*).
# Possible values:
# - Constant:     gaps.outer.top = 8
# - Per monitor:  gaps.outer.top = [{ monitor.main = 16 }, { monitor."some-pattern" = 32 }, 24]
#                 In this example, 24 is a default value when there is no match.
#                 Monitor pattern is the same as for 'workspace-to-monitor-force-assignment'.
#                 See: https://nikitabobko.github.io/AeroSpace/guide#assign-workspaces-to-monitors
[gaps]
inner.horizontal = 0
inner.vertical =   0
outer.left =       0
outer.bottom =     0
outer.top =        0
outer.right =      0

############################
# MAIN BINDINGS
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
############################
[mode.main.binding]

### LAYOUT ###
# See: https://nikitabobko.github.io/AeroSpace/commands#layout
alt-f = 'fullscreen'

alt-r = 'layout horizontal vertical' # [R]otate
alt-s = 'layout tiles accordion' # [S]tacked
alt-p = 'layout tiling floating' # [P]inned

alt-h = 'focus left'
alt-j = 'focus down'
alt-k = 'focus up'
alt-l = 'focus right'

alt-shift-h = 'move left'
alt-shift-j = 'move down'
alt-shift-k = 'move up'
alt-shift-l = 'move right'

alt-left = 'join-with left'
alt-down = 'join-with down'
alt-up = 'join-with up'
alt-right = 'join-with right'

alt-backspace = 'flatten-workspace-tree'

### RESIZE ###
alt-0 = 'balance-sizes'
alt-minus = 'resize smart -100'
alt-equal = 'resize smart +100'

### WORKSPACE ###
# Main screen uses bottom row: 'z' to '/'
# External screens use: 1 and 2
# See: https://nikitabobko.github.io/AeroSpace/commands#workspace
alt-1 = 'workspace 1'
alt-2 = 'workspace 2'
alt-z = 'workspace Z'
alt-x = 'workspace X'
alt-c = 'workspace C'
alt-v = 'workspace V'
alt-b = 'workspace B'
alt-n = 'workspace N'
alt-m = 'workspace M'
alt-comma = 'workspace cm'
alt-period = 'workspace pr'
alt-slash = 'workspace sl'

alt-shift-1 = 'move-node-to-workspace 1'
alt-shift-2 = 'move-node-to-workspace 2'
alt-shift-z = 'move-node-to-workspace Z'
alt-shift-x = 'move-node-to-workspace X'
alt-shift-c = 'move-node-to-workspace C'
alt-shift-v = 'move-node-to-workspace V'
alt-shift-b = 'move-node-to-workspace B'
alt-shift-n = 'move-node-to-workspace N'
alt-shift-m = 'move-node-to-workspace M'
alt-shift-comma = 'move-node-to-workspace cm'
alt-shift-period = 'move-node-to-workspace pr'
alt-shift-slash = 'move-node-to-workspace sl'

# back and forth between two most recent workspaces
alt-tab = 'workspace-back-and-forth'

### SECONDARY DISPLAY ###
alt-d = 'focus-monitor --wrap-around next' # [D]isplay
alt-shift-d = 'move-workspace-to-monitor --wrap-around next' # [D]isplay

[workspace-to-monitor-force-assignment]
# GIVEN: Built-in=B, External=E
# THERE ARE THREE CASES:
#   1. ONE MONITOR:
#     - E1=Main or B=Main: should use 'Z' to '/'
#   2. TWO MONITORS:
#     - E1=Main and B=secondary:
#       - workspace 1 is assigned to B
#     - E1=Main and E2=secondary:
#       - workspace 1 is assigned to E2
#   3. THREE MONITORS:
#     - E1=Main, E2=secondary, B=third: 
#       - workspace 1 is assigned to B
#       - workspace 2 gets assigned to E2

1 = ['secondary', '^built-in retina display$']
2 = 2
